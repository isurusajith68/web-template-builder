// Central client-side API base. Set `window.API_BASE` before this file loads to override.
// Default points to the production API gateway used by the templates.
window.API_BASE = window.API_BASE || "https://v2d.webtemplateapi.ceyinfo.com";

// Example: to use a local dev server, include a script before this one that sets:
// <script>window.API_BASE = 'http://localhost:3000'</script>
